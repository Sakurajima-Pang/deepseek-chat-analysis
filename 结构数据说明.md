# structure.json 结构数据说明

## 顶层字段

| 字段 | 类型 | 说明 |
|------|------|------|
| `meta` | object | 元数据：更新时间、视口约定等 |
| `tree` | object | 零件树，描述页面的嵌套层级 |
| `states` | object | 状态字典，描述每个零件有哪些状态 |

## 零件树 (tree)

每个节点包含：

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | string | 零件唯一标识，小写字母 + 连字符 |
| `label` | string | 中文名称，用于展示 |
| `selector` | string | CSS 选择器，用于定位该零件 DOM 节点 |
| `children` | array | 子零件列表，结构递归 |

层级关系：root → sidebar / main-panel → ...

## 状态字典 (states)

以零件 id 为 key，值为该零件的状态列表。

每个状态包含：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `name` | string | 是 | 状态名称 |
| `preconditions` | object | 否 | 前提条件：key=零件 id，value=该零件需处于的状态名 |
| `attrs` | object | 否 | 特征属性：用于区分该状态的 DOM 特征 |
| `note` | string | 否 | 备注：记录特殊情况说明，如多个同类元素无法通过 class 区分时的替代标识方法 |

### 前提条件 (preconditions)

- key 为其他零件的 id
- value 为该零件需处于的状态名
- 例子：`{ "sidebar": "展开" }` 表示"当 sidebar 处于展开状态时"

### 备注 (note)

可选字段，记录该状态的特殊情况说明。典型场景：
- 多个同级零件 class 和结构完全相同，无法通过 class 区分
- 记录替代标识方法的优先级（如 pathD > SVG 属性 > selector 位置）
- 维护提示

note 内容建议采用 `\n` 分行，每行一条关键信息。

### 特征属性 (attrs)

记录该状态下可观测的 DOM 特征，如：

| 属性 | 说明 |
|------|------|
| `classes` | 当前激活的 CSS class 列表 |
| `width` / `height` / `x` / `y` | 尺寸与位置 |
| `childCount` | 子元素数量 |

## 约定

- 零件 id 全局唯一
- 一个零件的不同状态之间，通过 `attrs` 组合区分
- 状态名在零件内部唯一
- `preconditions` 为空或不存在时，表示该状态无依赖，即"常驻状态"
